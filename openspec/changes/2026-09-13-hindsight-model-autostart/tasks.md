## 1. 前置核对与纯函数层（先写测试）

- [x] 1.1 前置核对：确认 `2026-09-13-hindsight-model-daemon-control` **已归档**——主 spec 里那条「插件 MUST NOT 自行启动、停止或重启守护进程」已被 `REMOVED`。验证：`openspec list` 不再列出该活跃变更，且 `grep -n "MUST NOT 自行启动" openspec/specs/hindsight-model/spec.md` 无命中；未满足则停止实现并回报。
- [x] 1.2 实现启动器形态判定纯函数：读启动器文件自身的可执行文件头，得出 `gui | console | unknown`（非 Windows、文件缺失、头不可解析一律 `unknown`）。验证：harness 用本机真实文件断言（venv 的 `pythonw.exe` → 非控制台形态）、用截断/伪造文件断言 `unknown`。
- [x] 1.3 实现自愈决策纯函数：`{form, guiSourceExists, replaceResult}` → `{action: 'none'|'replace', reason}`；`unknown` 一律 `none`。验证：harness 覆盖「console ⇒ replace」「gui ⇒ none」「unknown ⇒ none 且带原因」「替换失败 ⇒ 不得上报成功」。
- [x] 1.4 实现退避纯函数：失败档位递增（建议 1/5/15/30min 封顶）、成功即复位、同一失败不重复产出提示。验证：harness 用注入时钟断言档位与复位，并断言连续失败只产出一条提示。
- [x] 1.5 实现来源判定纯函数：输入（监听 PID、解释器镜像路径、内存中最近一次动作记录）→ `auto | manual | external | unknown`，并给出「是否带控制台窗口风险」。验证：harness 覆盖四态、控制台镜像追加风险标记、镜像取不到时为 `unknown`。

## 2. 宿主半：会话事件接入（能力探测 + 降级）

- [x] 2.1 订阅会话生命周期事件（`agent/session-start` / `agent/pre-step`）作为触发；宿主不提供该能力时降级为「仅手动」并记录「从未触发过」。验证：harness 用假 ctx 断言订阅被注册；不提供 `ctx.on` 时 apply 不抛错、手动启停仍可用。
- [x] 2.2 触发处理：健康探测（2s 超时）通过 ⇒ **采纳**（不写文件、不重启、不启动）；不通过 ⇒ 后台启动且不阻塞触发回调。验证：harness 注入假探测与假 spawn，断言「健康 ⇒ spawn 未被调用且无文件写入」「不健康 ⇒ spawn 恰好一次且未等待就绪」。
- [x] 2.3 退避接线：失败后按档位跳过后续触发，成功即复位。验证：harness 用假时钟连续触发，断言 spawn 次数符合档位变化，且成功后下一次触发立刻再试。
- [x] 2.4 开关接线：settings 键 `autoStart`（默认开）；关闭后触发直接短路。验证：harness 断言关闭后 spawn 一次都不发生；settings 服务缺失时用默认值且不报错。

## 3. 宿主半：启动前自愈与状态扩展

- [x] 3.1 启动前执行自愈，**手动与自动共用同一条路径**：形态为控制台 ⇒ 备份原件到 `.venv` 之外并用同一发行版的无控制台版本覆盖，然后启动；其余形态一律不写文件。验证：harness 用临时目录 + 假文件断言备份存在、覆盖内容正确、`unknown` 时不写任何文件；真机一次见 6.3。
- [x] 3.2 扩展状态端点：自动启动视图含触发次数、上次结果、退避剩余、来源、控制台窗口风险、开关态、是否发生过自愈。验证：harness 断言字段存在且随假场景变化，响应中不含密钥明文。
- [x] 3.3 自愈失败或取不到无控制台版本时：**不启动**并如实报告原因，且不得留下半换状态。验证：harness 断言 `ok:false` 且带原因、原文件字节未变。

## 4. 客户端半：开关与状态行

- [x] 4.1 面板新增「自动启动」开关（读写 settings；写失败时禁用并说明缺口）。验证：harness 渲染断言开关存在、切换发出请求、写失败时呈禁用态并显示原因。
- [x] 4.2 状态行新增文案：正在后台冷启动（约 44–73s）· 上次自动启动结果 · 来源（自动/手动/外部/未知）· 有控制台窗口风险 · 从未被触发。验证：harness 渲染断言各文案随状态出现，互不遮挡既有状态行信息。
- [x] 4.3 既有四个动作（启动/停止/重启/刷新）、二次确认与幂等语义保持不变。验证：既有用例全绿。

## 5. 文档与验收

- [x] 5.1 更新插件 `README.md`：自动启动的语义（采纳优先 / 后台 / 退避 / 开关）、自愈行为与回滚命令、来源与窗口风险的含义。验证：README 里每条命令、键名、开关默认值都从源码逐条核对。
- [x] 5.2 更新 `ACCEPTANCE.md`：新增 `[B]` 项（真机自动拉起、无控制台窗口、来源=自动、关掉开关后不再自动拉起、自愈发生时如实报告）。验证：本变更 delta 的每个 Scenario 都能对应到一条验收项。
- [ ] 5.3 `docs/plugins.md` 用法索引与 `docs/knowledge/` 篇目/索引登记（落盘前须用户点头）。验证：文件存在、索引行指向正确路径、描述与实现一致。

## 6. 回归与真机验证

- [x] 6.1 全仓库测试无新增回归。验证：`sub-plugins/*/test/*.test.mjs` 与 `dsh-plugin-manager/test/*.test.mjs` 输出；已知第三方红 `dsh-open-session-workdir/test/interception.test.mjs` 不计入本次回归。
- [x] 6.2 红绿验证：新增用例先在改前 bundle 上跑红，改后全绿。验证：临时写回改前文件跑一遍并恢复，`diff -q` 确认字节一致。
- [ ] 6.3 `[B]` 真机（部分完成：自动拉起/采纳/来源=自动 已由 2026-09-14 真机确认，见 ACCEPTANCE §11.1–11.2；余下 11.3/11.5–11.8 待手测）：停掉守护进程 → 触发一次会话 → 观察**后台自动拉起**、**无 `conhost`**、9077 监听者为无控制台镜像、面板来源=「自动」、四层仍 `applied=true` / `consistent`；再关掉开关重复一次，确认不再自动拉起。验证：用户手测；未通过则本变更不算完成。
