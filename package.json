{
  "name": "dsh-local-plugins",
  "version": "0.1.0",
  "description": "DEPRECATED (retired 2026-09-09) umbrella bundle of this monorepo — superseded by dsh-plugin-manager. Historical reference only: do NOT install this as a profile bundle and do NOT add new plugin rows/dependencies here. The manager (one profile dependency + one bundle layer) scans the repo root and keeps each dsh-* sibling's activation row in the profile's own cordis.patch.yml (live HMR). Kept in place so old backups/profiles can still resolve it; the dependency rows below are the historical roster.",
  "private": true,
  "type": "module",
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    }
  },
  "dependencies": {
    "dsh-composer-history-recall": "link:./dsh-composer-history-recall",
    "dsh-open-session-workdir": "link:./dsh-open-session-workdir",
    "dsh-session-title-regenerate": "link:./dsh-session-title-regenerate",
    "dsh-session-time-bucket": "link:./dsh-session-time-bucket",
    "dsh-esc-rewind": "link:./dsh-esc-rewind"
  }
}
