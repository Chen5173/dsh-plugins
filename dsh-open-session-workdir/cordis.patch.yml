# dsh-open-session-workdir bundle layer: inserts the open-workdir plugin row.
# Loaded via `dsh plugin --profile <name> add` — the row id stays stable so
# re-adds and profile patches can target it.
- insert:
    - id: open-session-workdir
      name: 'dsh-open-session-workdir'
