# 验收清单 — dsh-session-time-bucket

逻辑层（分桶边界、可见性过滤、前缀格式、行身份匹配、桶区间规划、相对时间、核心视图跟随判定、当前空白新会话钉今天、zh/en、bundle 契约）由 `test/bundle.test.mjs`（16 条）自动覆盖。标 `[B]` 的必须人眼看。

架构重点：**不重绘官方列表**。行（状态点、悬停卡、`…` 菜单、拖拽、打开）全部是核心原生元素，插件只注入组头 + 行内工作区前缀。

## 0. 前置

安装一律走管理器面板（先装 `dsh-plugin-manager` 一次 → 重启 `dsh web` → 设置 →「本地插件」→ 打开本插件主开关）。**不要**用 `dsh plugin --profile web add` 装本子插件（不会激活，见 README 警示）。

```bash
# 面板启用后应能查到本插件的激活行（管理器写入，rowId = session-time-bucket）
dsh --dump-config --profile web | grep -n session-time-bucket
```

- [ ] 0.0 `[B]` 面板启用后：profile `devDependencies` 出现本插件的 `link:`、`dsh.profile.bundles` 里本地条目仍**只有 `dsh-plugin-manager`**、`cordis.patch.yml` 里 `session-time-bucket` 行**只有一行**
- [ ] 0.1 `[B]` 刷新/强刷后，Console 无报错；核心侧栏一切如常（**没有任何额外入口图标**——本插件无独立入口）
- [ ] 0.2 `[B]` 核心“视图选项”菜单 = 核心原生内容（分组方式/排序方式），**无任何插件注入项**

## 1. 跟随激活（单列表 + 最近更新）

- [ ] 1.1 `[B]` 核心分组方式选「单列表」、排序方式选「最近更新」→ 自动激活：**列表本身仍是核心原样渲染**（不闪、不替换），行间/行内出现注入元素
- [ ] 1.2 `[B]` 组头：今天/昨天/前7天/前30天/更早（**空桶不出现**）出现在每桶首行上方；▾/▸ + 数量；hover 有底色；点击折叠（该桶行全部隐藏）/展开；刷新后折叠态保持
- [ ] 1.3 `[B]` 每行标题前有工作区前缀 `[工作区名] 标题` / `[未分组] 标题`（12px tertiary，不遮挡截断）；当前空白“新会话”行无前缀；已归档/子代理不出现（核心行为）
- [ ] 1.4 `[B]` 核心原生行为全保留：进行中蓝色追逐状态点、已完成绿点、悬停 0.5s 提示卡、行尾 `…` 菜单（重命名/分叉/归档）、点行打开、拖拽排序——与未激活时完全一致
- [ ] 1.5 `[B]` 进行中会话的 updatedAt 持续变化（行重排）时：组头始终跟随正确的桶边界行（观察者即时重锚，无明显错位/闪烁）
- [ ] 1.6 `[B]` 新建会话（含核心复用工作区里仍为空白的旧会话）→ 该“新会话”行始终落在 **今天** 桶且为桶内最新；一旦发出第一条消息仍留在 今天；切换到别的会话后旧空白行不再显示

## 2. 退出 / 还原

- [ ] 2.1 `[B]` 排序方式切回「手动排序」→ 自动退出：组头与前缀全部移除，核心列表原样保留（**不重渲染**）
- [ ] 2.2 `[B]` 分组方式切回「按工作区」→ 自动退出，核心树原样
- [ ] 2.3 `[B]` 激活态进入搜索 → 自动退出（搜索树接管）；关闭搜索且仍在 单列表+最近更新 → 自动恢复时间桶
- [ ] 2.4 `[B]` 窄栏（rail）下不激活，核心正常渲染
- [ ] 2.5 `[B]` 退出后原折叠隐藏的行全部恢复显示（无残留 display:none）

## 3. 持久化

- [ ] 3.1 `[B]` 折叠某桶后刷新/重开 → 折叠态保持；核心视图仍是 单列表+最近更新 时自动恢复时间桶
- [ ] 3.2 `[B]` 核心的 分组方式/排序方式 选择本身由核心持久化，插件跟随不受影响

## 4. 回归

- [ ] 4.1 `[B]` 核心“视图选项”菜单内容与操作完全不受影响（无注入项、不闪没、不双勾）
- [ ] 4.2 `[B]` 激活态下归档/重命名/新建/分叉会话（核心行菜单触发）→ 组头数量与边界即时更新、无重复组头、无残留
- [ ] 4.3 `[B]` 退出后核心列表无残留（无多余节点、无空行、标题 margin 还原）
