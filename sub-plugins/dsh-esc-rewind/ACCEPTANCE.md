# 验收清单 — dsh-esc-rewind

逻辑层（bundle 契约、决策矩阵、交换点推导/锚点、/rewind 选项与选择、fork/归档/打开调用序列、首轮降级、pending 还原、删除模式策略/降级/开关）由 `test/bundle.test.mjs`（28 条）自动覆盖。标 `[B]` 的必须在真实 GUI 人眼/手测。

架构重点：**回退 = fork 分支 + 按开关处置原会话**（默认归档，append-only 日志无原地删除；删除模式经宿主半真删）；armed 是派生状态（running 或 尾部 interrupted + 草稿为空）。

## 0. 前置

安装一律走管理器面板（先装 `dsh-plugin-manager` 一次 → 重启 `dsh web` → 设置 →「本地插件」→ 打开本插件主开关）。**不要**用 `dsh plugin --profile web add` 装本子插件（不会激活，见 README 警示）。

```bash
# 面板启用后应能查到本插件的激活行（管理器写入，rowId = esc-rewind）
dsh --dump-config --profile web | grep -n esc-rewind
```

- [ ] 0.0 `[B]` 面板启用后：profile `devDependencies` 出现本插件的 `link:`、`dsh.profile.bundles` 里本地条目仍**只有 `dsh-plugin-manager`**、`cordis.patch.yml` 里 `esc-rewind` 行**只有一行**
- [ ] 0.1 `[B]` 刷新/强刷 GUI 后 Console 无报错；`window.__dsew` 存在且 `applied:true`；核心一切如常
- [ ] 0.2 `[B]` 会话输入框输入 `/` → 命令菜单出现 `rewind`（描述为中文/英文对应 locale）

## 1. Esc 停止

- [ ] 1.1 `[B]` 生成进行中按 `Esc` → 生成停止（等同 Stop 按钮），toast「已停止 · 再按 Esc 回退本轮」；`window.__dsew.escStops` 递增
- [ ] 1.2 `[B]` 用工具栏 Stop 按钮停止 → 出现同款“再按 Esc 回退”提示（一次/回合）
- [ ] 1.3 `[B]` 有弹层/菜单（如命令菜单、设置、图册）打开时按 `Esc` → 先关弹层，**不**触发停止；再按才停止（层叠语义）
- [ ] 1.4 `[B]` 输入法（IME）组合中、或焦点在非 composer 文本框（如侧栏搜索）里按 `Esc` → 不触发停止

## 2. Esc Esc 回退

- [ ] 2.1 `[B]` 停止后（尾部为“已停止”半截回复）再按 `Esc` → 该整轮（含半截回复/工具消息/排队消息）消失：fork 出新分支并在最前打开，**原会话从列表消失（已归档）**，标题与原来一致（无“副本”后缀）
- [ ] 2.2 `[B]` 分支会话输入框**已还原**被撤销的问题文本；直接改写后发送，模型上下文从上一完整回合开始（不会重复旧内容）
- [ ] 2.3 `[B]` 生成中**快速连按两次** `Esc` → 直接完成“停止+回退”（无需等停稳），结果同 2.1
- [ ] 2.4 `[B]` 带图片的问题被回退 → 输入框尽力还原图片（图片缩略图出现）；图片读取失败的场景降级为纯文本、不报错
- [ ] 2.5 `[B]` 自然正常结束的回合按 `Esc`（一次或两次）→ 无任何动作、光标行为正常（不误删）
- [ ] 2.6 `[B]` 停止后先在输入框打了字再按 `Esc` → 不回退（不覆盖新草稿）
- [ ] 2.7 `[B]` 停止后发送了新消息或切换了会话再按 `Esc` → 不回退
- [ ] 2.8 `[B]` 回退后原会话可从“归档/隐藏”视图恢复（日志保留）——非破坏性
- [ ] 2.9 `[B]` 会话第一条消息生成中被停 → `Esc`② → 打开同工作区**新空会话**并还原问题；原空会话归档

## 3. /rewind 命令（原生选择器 + 全量预读）

- [ ] 3.1 `[B]` 输入框 `/rewind`（菜单选中或回车）→ 打开**原生命令选择器**；打开瞬间显示“加载中”，随后列出全部历史
- [ ] 3.2 `[B]` 行 = 序号（最新为 1）+ 用户提问截断文本 + 相对时间（纯图片消息显示占位），**最新在最上**、逐条向旧排列
- [ ] 3.3 `[B]` 会话历史很长、聊天从未渲染更早内容时：选择器打开后**一次性把全部历史读出来**，最旧的“第 1 轮”也在列表里可选中（不再按 ↓ 没反应、只有几条）
- [ ] 3.4 `[B]` `↑/↓` 移动高亮、回车选中；在搜索框打字可过滤（原生行为）
- [ ] 3.5 `[B]` `Enter`（或点击某行）→ 从该轮之前重开：fork+归档+打开+还原该轮问题（同 2.1/2.2）；会话正文/列表正确切换
- [ ] 3.6 `[B]` 选择“第 1 轮（最早）” → 打开同工作区新空会话并还原该问题（首轮降级）
- [ ] 3.7 `[B]` 长会话打开 `/rewind` 命令**始终可选**（不因更早内容未加载而消失）；空白/子代理会话不出现
- [ ] 3.8 `[B]` 生成进行中调 `/rewind` → 回退执行时自动停止
- [ ] 3.9 `[B]` 选择器打开时按 `Esc` → 关闭且不做任何事；关闭后再按 `Esc` 才回到停止/回退语义
- [ ] 3.10 `[B]` **load-once**：同一页面内第二次/多次打开 `/rewind` 不再出现“加载中”、秒出全列表（`window.__dsew.historyLoads` 只 +1）
- [ ] 3.11 `[B]` **刷新页面后**再开 `/rewind`：无新消息时直接从缓存出列表、不再全量请求（Console/网络无 loadThrough）；有新消息（你又发了几轮）时合并重载一次，**最新回合出现在列表顶部**
- [ ] 3.12 `[B]` 从缓存选择很早的回合回退仍正确（fork 锚点/文本/图片均来自记录）；超长会话缓存过大时自动退化为仅本页内存缓存，不影响功能

## 4. 回归与稳定性

- [ ] 4.1 `[B]` 核心会话行 `…` 菜单（重命名/分叉/归档）、Stop 按钮、发送/排队、标题自动生成等核心行为不受影响
- [ ] 4.2 `[B]` 长时间运行无重复 toast/无限重试；连续多次回退均成功且分支无重复
- [ ] 4.3 `[B]` 无报错时 `window.__dsew.lastGate` 能解释每次“为什么没接管”（诊断只含计数与门控，不含消息内容）

## 5. 删除模式（开关 + 宿主半真删）

前置：`src/index.js`（宿主半）改动后已重启 GUI host；`window.__dsew.deleteModeLoaded === true`。

- [ ] 5.1 `[B]` 会话头右侧图标排出现处置开关：默认**档案柜**（灰）；悬停 title 为「回退后归档旧会话（可恢复）」
- [ ] 5.2 `[B]` 单击开关 → 图标变为**红色带叉垃圾桶**，toast「已开启：回退将删除旧会话（不可恢复）」；再点切回档案柜 + toast「已关闭：回退将归档旧会话」
- [ ] 5.3 `[B]` 开关与 chameleon「删除当前会话」垃圾桶**视觉可区分**（本开关红色带叉、语义不同）；两者同排互不遮挡
- [ ] 5.4 `[B]` 子代理会话头与空白会话头**不显示**该开关；普通会话头显示
- [ ] 5.5 `[B]` 删除模式下：Esc 停止 toast 为警示文案「已停止 · 再按 Esc 将删除本轮并重来（不可恢复）」（归档模式下为普通文案）
- [ ] 5.6 `[B]` 删除模式下 Esc-Esc 回退：fork 出新分支并打开 → **原会话从列表消失且磁盘日志被删**（`~/.dsh/sessions/` 下该 id 目录不存在），toast「旧会话已删除」；归档/隐藏视图里也找不到
- [ ] 5.7 `[B]` 删除模式下 `/rewind` 选中历史回合 → 同样真删原会话（含首轮降级路径：旧空会话被删、新空会话打开）
- [ ] 5.8 `[B]` 删除失败降级：临时停掉/破坏宿主删除通道（或模拟权限拒绝）后回退 → 旧会话**仍被归档**（可从归档视图恢复）+ toast「删除失败，已改为归档」；新分支不受影响
- [ ] 5.9 `[B]` 开关状态**全局持久化**：设置页 `esc-rewind` 段可见 `deleteOldOnRewind`；在任一会话头切换后刷新页面，图标仍为删除态；设置页直接改该字段后（不刷新）会话头图标即时刷新
- [ ] 5.10 `[B]` describe 读不到/报错时开关回退归档态（`window.__dsew.deleteMode === false`），绝不误开删除
- [ ] 5.11 `[B]` 卸载插件后：settings.yaml 中手写的 `esc-rewind:` 段若存在则无害残留（README 说明清理）；已删除的会话不可恢复（功能语义）
- [ ] 5.12 `[B]` 与 `@huanlin/dsh-plugin-session-delete` 共存：各自按钮/开关正常，删除当前会话（chameleon）与回退处置旧会话（本插件）互不干扰
