# dsh-composer-history-recall bundle layer: inserts the composer-history-recall plugin row.
# Loaded via `dsh plugin --profile <name> add` — the row id stays stable so
# re-adds and profile patches can target it.
- insert:
    - id: composer-history-recall
      name: 'dsh-composer-history-recall'
