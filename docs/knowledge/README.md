# docs/knowledge — 开发知识库

本目录沉淀 **dsh-plugins 开发中可复用的结论**（决策、接口、踩坑、验证方法），供跨会话检索。每完成一段开发就新增一篇 `YYYY-MM-DD-<功能slug>.md`，**并在下方表格追加一行**，避免文档成为孤岛。

写作纪律：宁短勿长、只写可复用结论、不复制大段代码、已有 README/ACCEPTANCE 覆盖的内容引用路径即可。

## 索引

| 日期 | 主题 | 文件 | 一句话要点 |
|---|---|---|---|
| 2026-09-06 | DSH 插件：会话「重新生成标题」 | [2026-09-06-dsh-session-title-regenerate.md](2026-09-06-dsh-session-title-regenerate.md) | 外部插件用 commands.execute 触发 host 逻辑 + viewport 内 DOM 注入菜单项；最低推理=off+session-title；remote.commands 必须注入读；session.events 跨版本兼容 |
| 2026-09-06 | DSH 插件：侧栏「按时间桶」分组 | [2026-09-06-dsh-session-time-bucket.md](2026-09-06-dsh-session-time-bucket.md) | v3 跟随核心视图 + 就地增强：读核心持久化视图状态 localStorage `dsh.workspace.view.v5`（groupBy/orderBy 整值 JSON 每次变更同步写回），单列表+最近更新时**不重绘官方列表**，只在行间注入时间桶组头、行内注入 `[工作区]` 前缀，官方行（状态点/hover卡/行菜单/拖拽）全保留；React 按 fiber 调和只动自己节点，注入节点安全，行重排时用 MutationObserver 即时重锚组头；不注入核心分组菜单（portal 两阶段渲染不可安全扩展） |
| 2026-09-08 | DSH 插件：Esc 停止 / Esc Esc·/rewind 回退重来 | [2026-09-08-dsh-esc-rewind.md](2026-09-08-dsh-esc-rewind.md) | append-only 无法原地删消息：回退 = `sessions.fork(atSeq)`+`workspaces.archiveSession`+open+setDraft；纯客户端可注入 sessions/workspaces/conversation/uiConversation/commandUi(popupSelect 贡献=斜杠命令交互缝)；`inputActions` 无 cancel 用 `binding().session.cancel()`；armed=派生态（running 或尾部 interrupted 且草稿空）；document capture Esc 须让位弹层/trigger token；**v5 删除模式**：客户端无真删 verb，宿主半自建（`fs.rmSync` 日志目录+`storageDomain` 清投影/记账），settings 段 `esc-rewind.deleteOldOnRewind` 全局开关（describe 失败回退 false），会话头图标两态（档案柜⇄红叉垃圾桶），安全时序=新分支可用后才删、失败降级归档 |
| 2026-09-09 | DSH 插件：host 半禁止 import 宿主内部包（去核心化） | [2026-09-09-plugin-host-half-no-core-import.md](2026-09-09-plugin-host-half-no-core-import.md) | `link:` 插件真实路径在仓外 → Node 裸包解析从仓库向上找不到宿主内部包（默认解引用 symlink，`--preserve-symlinks` 才走 profile）→ 启动 `ERR_MODULE_NOT_FOUND`；host 半只用注入服务、宿主工具就地实现；曾靠不入库本地桩顶着的 title-regenerate 已去核心化 |
| 2026-09-09 | DSH 插件：composer 提供方标签升级为两级选择器 | [2026-09-09-dsh-composer-provider-label-picker.md](2026-09-09-dsh-composer-provider-label-picker.md) | 写路径只有 `session.selectModel` 且 provider+model 必须成对（宿主顺带写 profile 默认）；`Menu` 的 submenu 不可控/无勾选/关滚动上限 → 自管 pane + 原地 `side=top`；single 槽对动态插件是负优先级=遮蔽核心；link: 下 schemastery 解析失败 → settings 段是死的，偏好存 localStorage；目录失败只在 idle 隐式重试，其余走显式重试入口 |
| 2026-09-09 | DSH 插件：本地插件管理器（dsh-plugin-manager） | [2026-09-09-dsh-plugin-manager.md](2026-09-09-dsh-plugin-manager.md) | 只装一个 bundle（子插件放 devDependencies + profile `cordis.patch.yml` 管理器维护行，live HMR 实时生效）；宿主从 `ctx.baseUrl` 定位 profile、js-yaml 走 profile 顶层 node_modules、行 id 全局唯一、整文件重写先例=mcp-manager；客户端 `settings.section` order16 + `__ModuleLoader__.load` factory（require('react')，无 JSX）；客户端 UI 变化需一次页面刷新；legacy 布局需先一键迁移；根伞包 `dsh-local-plugins` 退役（2026-09-10 已删除） |
| 2026-09-10 | DSH 插件：子插件收拢到 sub-plugins/（布局重构） | [2026-09-10-sub-plugins-layout.md](2026-09-10-sub-plugins-layout.md) | 拆「仓库根 / 子插件根」两概念（`pluginRootsOf` 双根并集 + `pluginAbsDirOf` 嵌套优先）；行 id 只由目录名决定 ⇒ 布局迁移零激活变更；`link:` 陈旧由 `staleLinkSpec`+`ensureDevDep` 自愈；宿主半改动需重启 `dsh web` 才生效；伞包/根目录替换方案未采用的理由已记录 |
| 2026-09-10 | DSH 插件：一次开关 = 一次核心配置重应用（写入合并） | [2026-09-10-toggle-write-batching.md](2026-09-10-toggle-write-batching.md) | 写 profile `cordis.patch.yml` 会触发核心整树事务性重应用，独占宿主事件循环 **0.7–1.2 s**（相同字节写入零阻塞 ⇒ 代价在「配置变更」本身）；不改写挂载（SSE 未断）；插件侧只能减少**写入次数**（400 ms 意图去抖合并）并让该窗口可读（`pendingWrites` + 「正在应用」+ 不锁面板）；代价链路 `vendor/hmr` chokidar → `watchUserPatches` → `vendor/include` `root.update` |
| 2026-09-10 | DSH 插件：本地插件面板批量全部开启/全部关闭 | [2026-09-10-toggle-write-batching.md](2026-09-10-toggle-write-batching.md)「下一步」+ `openspec/changes/archive/2026-09-10-batch-toggle-writes/` | ⚠️ 本篇原索引指向的 `2026-09-10-batch-toggle-all.md` 与 `test/batch-toggle.test.mjs` **都未入库**（批量目前只有 `batchPlan` 纯逻辑 + 面板测试；「恰好一次写入 / 恰好一次 pnpm install / 逐项失败回报」三条缺 handler 级自动化）。可复用要点仍成立：一次点击 = 一次 patch 写入 + 一次 pnpm install；合并窗口里的单行意图要「接管」而非「先 flush 再批量」；按钮数量口径唯一源 = 宿主 `batchCounts`（`batchPlan`）；只动 disabled/active + 未安装补装；inactive/legacy/invalid 一律跳过 |
| 2026-09-11 | DSH 插件：用 git 地址安装整个 monorepo（根安装外壳落地） | [2026-09-11-install-via-git-url.md](2026-09-11-install-via-git-url.md) | `dsh plugin add git+https://…` 时 pnpm 把 **clone 根目录当作包** ⇒ 根 `package.json` 的 `name` 就是插件身份；无根清单则退化叫 `dsh-plugins`/`pre.git` 且被 reconcile 判成普通依赖、永不激活。外壳=同名转发（`main`/`exports`/`dsh.client`/`dsh.bundle.patch` 指回子目录）、零内容（无依赖、无 `dsh.profile`、根不放 `cordis.patch.yml`）；git 依赖带整棵工作树 ⇒ 根清单 MUST NOT 设 `files`；`repoRootOfPluginSrc` 天然适配 clone；`link:` 指向 clone 内 `node_modules` 可被 pnpm 接受并解析；git 装是快照（`update` 升级）；验证用 `DSH_HOME` 临时目录 + 本地 bare remote + `git+file://`，断言走 `--dump-default-config`/`--dump-config`（不起服务） |
| 2026-09-12 | esc-rewind 快捷键回退放宽到「尾部非 settled」（v6，见 esc-rewind 篇末） | [2026-09-08-dsh-esc-rewind.md](2026-09-08-dsh-esc-rewind.md) | 非运行态下只要尾部非 settled（含刚发出的 user 提问）且草稿为空即允许 Esc 回退；新增 `tailUnsettled(list)` 统一 armed 判定与 hint；运行态两次 Esc、草稿非空 disarm 不变；37/37 harness 全绿 |
| 2026-09-12 | esc-rewind 非运行态也两次 Esc（v7，见 esc-rewind 篇末） | [2026-09-08-dsh-esc-rewind.md](2026-09-08-dsh-esc-rewind.md) | 非运行态防误触：第一次 Esc 返回 `action:'arm'`（记 stopMarkRef + 提示），第二次（`stopIssued`）才 `rewind`；新增 `arm` 分支不调 `issueStop`；复用既有 `stopMarkRef`/`stopIssued` 不新增状态；37/37 全绿 + 全仓库无回归 |
| 2026-09-12 | esc-rewind 提示时机改用宿主耐久停止证据（v7.2，修「自然结束仍弹提示」） | [2026-09-08-dsh-esc-rewind.md](2026-09-08-dsh-esc-rewind.md) | v7.1 的「running 下降沿」判据不成立：自然结束同形；且 `running`（api-session/status 广播）与 `chat.legacy.nodes`（事件 journal + 3 帧发布）是两个 store，而**运行中的 assistant 行在 legacy 投影里不产出节点** ⇒ 下降沿那一帧尾巴是本轮 user 提问 ⇒ 误弹。改为「下降沿记候选 + 该轮定型后按耐久证据结算」：尾部 `interrupted`（`agent-loop` 仅取消时写）或 `turn/end` 为 `aborted`/`user`（覆盖无内容即停；`turn/end` 的 `interrupted` kind 是崩溃修复标记，不能当停止证据）；新增 `tailStatus`/`lastTurnEndEvidence`、`__diag.hintToasts`/`lastHint`；49/49 全绿，新用例在 HEAD 版 client.js 上 8 条全红 |
| 2026-09-10 | DSH 插件：删伞包 + 子插件去 `dsh.bundle`（安装路径唯一化） | [2026-09-10-retire-bundle-install.md](2026-09-10-retire-bundle-install.md) | 五种「配置链坏了就起不来」的真实报错（`failed to import loader entry` / `cannot resolve profile bundle` / `declares no dsh.bundle` / `duplicate loader entry id`）；**duplicate 闸门先于包解析** ⇒ 同 id 无条件崩溃（`applyEntryPatches` 不去重）；`dsh.bundle` 声明 = 会不会被 reconcile 塞进 bundles 的总开关；CLI `add` 只装不激活、CLI `remove` 会留悬空行导致启动失败 ⇒ 安装/卸载只能走管理器面板 |
| 2026-09-12 | 宿主 0.1.5-rc.2 版本适配：全量契约审计 + 草稿附件改名（esc-rewind v7.3） | [2026-09-12-dsh-015-core-api-compat-audit.md](2026-09-12-dsh-015-core-api-compat-audit.md) | 审计方法是「按文件对撞公开成员名」：`git diff --name-only <old-tag>..<new-tag>` 圈出变更文件 → 每文件抽 `^\s+(readonly )?name(:|\()` 求两 tag 差集 → 只对**去掉成员**的文件继续看代码。结论：0.1.2-rc.1 → 0.1.5-rc.2 之间本仓库插件触点**只有草稿附件一族改名**（`conversation.createDraftImages(files)`→`createDrafts(sessionId, files)`、`releaseDraftImage`→`releaseDraftAttachment`、`inputActions.addImages`→`addAttachments`；`InputState.imageIds`→`attachments` 本仓库未用），槽位名与标准 props（只新增 `useResource`/`usePanelInfo`）、客户端服务名、`chat.legacy.nodes`/`timeline.turnOrder|turns`、`turn/end` 的 `aborted`/`user`、`assistant/message.interrupted`、侧栏 CSS 类名与 `role=tree`、`dsh.workspace.view.v5`、profile `cordis.patch.yml`/`dsh plugin` CLI **全部未变**；应对方式是**能力探测两代兼容**，不是读版本号分支。⚠️ **该结论不完整**：它漏掉了类型级契约变更（见下一行 v7.4），方法已按 v7.4 补上「类型签名差分」这一步 |
| 2026-09-12 | 宿主 0.1.5-rc.2 版本适配：真机两个缺陷的根因与修法（esc-rewind v7.4） | [2026-09-08-dsh-esc-rewind.md](2026-09-08-dsh-esc-rewind.md) 末节 | ①`/rewind` 消失 = **类型级契约变更**：`CommandContribution.description` 由 `string` 变 `() => string`（核心会调用它），字符串被调用即抛 `TypeError` 拖垮整个候选装配；只对撞成员名抓不到，已补「类型级差分」进审计流程；旧核心把该值当 React 子节点渲染 ⇒ 双形态无法共存，改用 getter + 能力探测（`main.conversation` 槽 / 新标准 props `usePanelInfo`·`useResource`）。②「同一条消息被执行、又有一条在等待」= **fork 的事件种子把父会话未落定的排队输入复制进子分支**（0.1.5 把 agent 收件箱写进日志：`agent/inbox/spliced`，日志里 `#79 插入 pending A → #80 session/end-seed{inherited:true} → #85 A 被执行`）；修法是 fork 前「删排队项 + 等快照确认」，清不掉就放弃回退。取证要点：会话日志是多帧 zstd，**必须 `read_across_frames=True`**（Node 同步解压只出第一帧，会误判成 1 行） |
| 2026-09-13 | DSH 插件：Hindsight 模型面板（dsh-hindsight-model） | [2026-09-13-dsh-hindsight-model-panel.md](2026-09-13-dsh-hindsight-model-panel.md) | Hindsight 是独立 Python 守护进程，其权威 `.env` 会被**启动路径自己回写**（`cli.py` 不覆盖已存在键 → `{**profile,**config}` → `_register_profile` → `create_profile` 整体重写）⇒ 外层同名环境变量赢；写入只能行级 read-modify-write、绝不用 `profile create`；**生效判据不能用「StartTime > mtime」**（实测文件 18:56:29 晚于进程 18:55:46 而取值一致 ⇒ 健康配置被误报未生效），正确主判据是「文件三件套 vs 启动日志里进程实际读到的三件套」；DSH 侧 `agentDefaultModel.currentSelection()` 是**方法**、provider 的 `baseURL` 只能 `listConfigurableProviders()`+按 `settingsPath` 下钻、明文密钥只有宿主半能取；⚠️ 交接稿的 `..._EMBEDDINGS_MODEL`/`..._RERANKER_MODEL` **不存在**，真名带 `_LOCAL_`；`registerHttp` 只是 esc-rewind 的局部函数名，真 API 是 `ctx.webServer.register` |
| 2026-09-13 | esc-rewind v7.5：**v7.4 只修了一半** —— fork 种子必然带出被回退那条消息 + 还原输给 React 提交 | [2026-09-08-dsh-esc-rewind.md](2026-09-08-dsh-esc-rewind.md) 末节 | ①主因是**结构性泄漏**不是竞态：宿主 `fork` 的 `cut` 从边界 `turn/end` 一路走到**下一个 `turn/start`**，而被回退消息的 `agent/inbox/spliced`(insert) 恰好在它自己的 `turn/start` 之前、claim 之后 ⇒ 种子只带 insert 不带 claim ⇒ 子会话的**耐久 `inbox` 投影**里有一条继承来的 pending 旧消息，重发时先执行它、新消息排队；②守卫只读 queue 镜像，而 queue 帧只在「有活跃 agent」时广播（`inbox` 投影帧无条件广播），且刚回车的输入还只在本地回声 `pendingSubmissions` 里 ⇒ 看不到；③「文字没回输入框」= `armPendingRestore` 排在 `open`+一次会 sleep 的 await 之后，子会话先挂载时 effect 读到 null 且只按 `[sessionId,draft,inputActions]` 重跑 ⇒ 永久丢（harness 旧用例在 doRewind 结束后才挂载，正好掩盖）；修法=三来源守卫（投影∪镜像∪回声）+ fork 前清父会话 + open 后清子会话继承项 + 武装提到 open 前 + 模块级晚武装通知 + 「已回填的会话 id」代替一次性 ref；68/68，新 8 条在 v7.4 代码上全红 |
| 2026-09-13 | provider 标签改成胶囊 chip：先搞清「这个框是谁画的」 | [2026-09-13-provider-label-chip-chrome.md](2026-09-13-provider-label-chip-chrome.md) | composer 里那枚深色方框**不是插件画的**——插件写的是内联 `background: transparent`，内联压得住普通样式表 ⇒ 还能看见底色就必有 `!important`（或另一个元素在画）；顺着「截图取像素 `rgb(22,37,72)` = `#17264a`×0.9 叠在 `#0f1a33` 上」→ 皮肤 `skin.css` 的 `--dsw-alias-interactive-bg-hover-solid` → `grep !important` 命中 ice-princess `patches.css` 的 `[data-composer-card] button:not([role=menuitem]):not([role=option])`（只设底色 ⇒ 方角）。修法=**几何归插件、颜色归主题/皮肤**：抄核心 `ModelSelect.module.css .trigger` 的 28px 高 + 24px 胶囊 + 左右 10px 内边距（比它宽 2px）、`box-sizing: border-box` 让省略号上限含内边距，底色只写 token 让皮肤赢；验证=2 条新断言在 HEAD 版 client.js 上全红、改后 55/55，客户端半改完刷新页面即生效 |
| 2026-09-13 | Hindsight 面板 v2：守护进程启停，与两个互为镜像的上游 Windows 缺陷 | [2026-09-13-dsh-hindsight-model-panel.md](2026-09-13-dsh-hindsight-model-panel.md) 末节 | `hindsight-embed daemon stop` 在本类机器上**不可能工作**：`_run_probe` 用 `subprocess.run(text=True)` 按 locale 解码，而 venv 跑在 UTF-8 模式 ⇒ 拿 utf-8 解 Windows `netstat` 的 **cp936** 字节 ⇒ `Could not find PID` 拒绝停止；设 `PYTHONUTF8=0` 又让 `cli.py:108` 的 `open(profile)`（**未指定 encoding**）用 gbk 去读 UTF-8 的 profile ⇒ 崩。**开也错、关也错**。故面板自建停止：取监听端口 PID → 校验命令行含 `hindsight_api.main`＋`--daemon`＋`--port` 匹配（实测身份串）→ 才 `Stop-Process`（实测 234ms），保留上游「不向无法确认的进程发信号」的安全性质；启动仍走官方 CLI 并在 spawn 时**净化子进程 env**（否则外层变量会被写回覆盖 profile）。另：**证伪**「PROVIDER 启动后消失是净化造成」——实测保存→停止→启动后它存活且判据 `consistent`，真凶最可能是官方插件用缺键配置走了 `profile create --merge`（`render_config` **只把传入字典里有的键写成活动行**）；判据也修成「只比较文件里确实存在的字段」 |
| 2026-09-14 | Hindsight 面板 v2.1 + v3：启动路径的控制台窗口（1 个文件修好）与按需自动启动 | [2026-09-13-dsh-hindsight-model-panel.md](2026-09-13-dsh-hindsight-model-panel.md) 末两节 | 窗口的真凶是 **uv 建的 venv 把 `Scripts\pythonw.exe` 写成了控制台 launcher**（与同目录 `python.exe` 字节相同、PE 子系统 **CUI(3)**；uv 模板里那份才是 GUI(2)）——上游只检查该文件「存在」+ 用 `DETACHED_PROCESS` 摘掉父控制台 ⇒ Windows **新开**一个（conhost 的父进程就是 daemon）。**判据=PE 子系统，2 好 3 坏**；对照实验必须在非 bash 的宿主里做（bash 自带控制台会继承）。修法是**只换那 1 个文件**（备份到 `.venv` 之外，`uv run` 不会改回）。v3：**按需自动启动**归本插件（官方那条的 `detectLlm()` 只看宿主 env；且它 `spawn(uv)` 不隐藏窗口、`daemonEnv()` 会把宿主 env 交给 `profile create --merge` 写回）——触发只用 `agent/session-start`（不碰 `pre-step` 中介件），采纳优先、后台冷启动、退避 1/5/15/30min、启动前按 PE 子系统自愈 launcher（原子替换、失败拒绝启动）、来源只用「端口 PID+镜像+本进程做过什么」判定。⚠️ 宿主 settings 陷阱：`installSection()` **不返回句柄**，要写必须 `register()`→`scope.update()`，且重复注册会报错（需缓存扛 HMR）。真机：00:04:39 触发 → 00:04:41 起 → 00:05:19 就绪，无 conhost、profile sha256 未变、adopt 分支经探针验证 |
| 2026-09-18 | DSH 插件：批量「全部移除」（迁移后重新定位） | [2026-09-18-batch-remove-all.md](2026-09-18-batch-remove-all.md) | 根因：`linkSpecOf` 只写绝对 `link:`，`deriveStates` 只看键在不在 ⇒ 迁移后的陈旧链接显示「未激活(仅依赖)」且单行开关的 `present` 守卫让它**永不自愈**（`staleLinkSpec` 在该路径是死代码）；「全部移除」= 清空痕迹后重新启用走完整 `ensureDevDep` 按当前目录重写。顺序与批量开关**刻意相反**：丢弃排队意图（回报 `discardedIntents`）而非接管 → 1 次 patch 写 + 1 次 manifest 写 + 1 次 `pnpm install` → 失败双回滚；计数口径=有痕迹（active+disabled+inactive）。测试手法：替换共享 `node:fs.writeFileSync` 精确计写入次数（胜过 mtime 采样）+ `__setPnpmRunner` 桩 + 从真 profile junction `js-yaml` 到临时 `$DSH_HOME`；两次变异实跑验证「恰好一次」断言真会咬人 |
| 2026-09-18 | DSH 插件：退役 `dsh-open-session-workdir` + 「官方打开」探测不到终端的三条根因 | [2026-09-18-retire-open-session-workdir.md](2026-09-18-retire-open-session-workdir.md) | 管理器**没有插件清单**（`listRepoPluginDirs` 扫目录）⇒「不再显示」= 加 `RETIRED_PLUGIN_DIRS` 扫描排除，源码/文档/测试全保留（删目录丢参考、改名断链、只注掉 `slots.register` 面板仍列出）；**核心已自带同功能**（`ui-open-in-app` 在 `conversation.session.header.utilities` 的分体按钮 + 路由 `/open-in-app/{apps,icon/<id>,open}` + `dsh.open-in-app.choice`）；菜单里没有 Windows Terminal / PowerShell / cmd = 两个**独立**原因：目录表根本没有 powershell/cmd 条目，且 `wt.exe` 是 WindowsApps 的 **0 字节 app-exec alias**（`lstat` OK / `stat`+`realpath` `EACCES` / `access(X_OK)` OK）而 `resolveExecutable` 用 `stat`+`isFile` ⇒ 判成「没装」；验证手法 = `node --experimental-transform-types` 直接跑核心 `resolver.ts` 的 `resolveOpenInAppApps()`；⚠️ 本 harness 的 `pwsh` 是 **PS 5.1**，`Get-Content -Raw`/`Set-Content` 会把无 BOM UTF-8 的中文注释写成乱码，改文件一律用 `edit`/`write` |
