# dsh-session-title-regenerate bundle layer: inserts the plugin row.
# Loaded via `dsh plugin --profile <name> add` — the row id stays stable so
# re-adds and profile patches can target it.
- insert:
    - id: session-title-regenerate
      name: 'dsh-session-title-regenerate'
