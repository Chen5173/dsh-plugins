# 验收清单 — dsh-session-time-bucket

逻辑层（分桶边界、可见性过滤、前缀格式、相对时间、空桶隐藏、zh/en、bundle 契约）由 `test/bundle.test.mjs`（10 条）自动覆盖。标 `[B]` 的必须人眼看。

## 0. 前置

```bash
cd <dsh-plugins 仓库根>
dsh plugin --profile web install
```

- [ ] 0.1 `[B]` 重启/强刷后，宽侧栏“视图选项”图标旁出现一个**时钟入口**；打开核心“视图选项”菜单 = 核心原生内容（分组方式/排序方式），**无任何插件注入项**
- [ ] 0.2 `[B]` Console 无报错

## 1. 入口弹层（spec：分组方式 → 按时间桶 → 显示工作区）

- [ ] 1.1 `[B]` 点时钟入口 → 弹出自绘菜单：`分组方式` + `按时间桶`（未选中）
- [ ] 1.2 `[B]` 点 `按时间桶` → 出现对勾，下方出现横线与 `显示工作区` 勾选行
- [ ] 1.3 `[B]` 不勾选、点空白/Esc → 弹层关闭，核心无任何变化
- [ ] 1.4 `[B]` 勾选后点空白/Esc → 激活：核心这一节头部+列表被接管，换成插件界面（迷你头部 + 时间桶列表）

## 2. 时间桶接管界面

- [ ] 2.1 `[B]` 迷你头部依次是：时钟入口 · `按时间桶` · `显示工作区` 勾选 · `＋新建会话` · `×退出`
- [ ] 2.2 `[B]` 列表：今天/昨天/前7天/前30天/更早（**空桶不出现**）；组标题带 ▾/▸ 与数量，hover 有底色，点击折叠/展开且刷新后保持
- [ ] 2.3 `[B]` 行尾相对时间（如 “5天前”）；`显示工作区` 开 → `[工作区名] 标题`、未分组 `[未分组]`，关 → 纯标题
- [ ] 2.4 `[B]` 已归档/子代理不出现；当前空白“新会话”出现在它自己的桶
- [ ] 2.5 `[B]` 点行打开会话（高亮随 current）

## 3. 激活态入口与退出

- [ ] 3.1 `[B]` 激活后点迷你头部时钟 → 弹层显示 `按时间桶` 已选中 + `显示工作区`（切换即时生效）
- [ ] 3.2 `[B]` 弹层里取消 `按时间桶` → 退出回核心（核心头部/列表恢复）
- [ ] 3.3 `[B]` 点迷你头部 `×` 同样退出；`＋` 新建会话后落在今天桶

## 4. 持久化与宽窄

- [ ] 4.1 `[B]` 开着（含 显示工作区、某桶折叠）刷新 → 自动恢复
- [ ] 4.2 `[B]` 接管状态下收窄侧栏 → 自动退出回核心；拉宽不自动重进

## 5. 回归

- [ ] 5.1 `[B]` 核心“视图选项”菜单内容与操作完全不受影响（无注入项、不闪没、不双勾）
- [ ] 5.2 `[B]` 退出/刷新后核心分组方式、搜索、拖拽、行菜单如常；无重复时钟入口

