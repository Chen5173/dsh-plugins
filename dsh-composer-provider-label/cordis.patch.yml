# dsh-composer-provider-label bundle layer: inserts the provider-label plugin row.
# Loaded via `dsh plugin --profile <name> add` — the row id stays stable so
# re-adds and profile patches can target it.
- insert:
    - id: composer-provider-label
      name: 'dsh-composer-provider-label'
