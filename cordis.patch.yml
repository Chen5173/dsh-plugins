# DEPRECATED (retired 2026-09-09) — historical umbrella bundle layer of this
# monorepo. Superseded by dsh-plugin-manager: do NOT install this bundle and do
# NOT append new rows here. The manager keeps each plugin's activation row in
# the PROFILE's own cordis.patch.yml (live HMR) instead of this boot-only file.
# Rows below remain only so older profiles/backups keep resolving stably.
# Row ids stay stable (equal to the ids the standalone bundles used), so
# existing profile cordis.patch.yml overrides/disables keep targeting them.
- insert:
    - id: session-title-regenerate
      name: 'dsh-session-title-regenerate'
    - id: open-session-workdir
      name: 'dsh-open-session-workdir'
    - id: composer-history-recall
      name: 'dsh-composer-history-recall'
    - id: session-time-bucket
      name: 'dsh-session-time-bucket'
    - id: esc-rewind
      name: 'dsh-esc-rewind'
